<% 
  @page_title = 'New book'
  @include_js = 'books'
%>

<% has_similar = @similar_books and not @similar_books.empty? %>
<% if has_similar %>
  <ol class="booklist">
    <%= render :partial => "books/book", :collection => @similar_books %>
  </ol>
  <h3>If you anyway want to add your book submit form one more time.</h3>
<% end %>

<% form_for(@book) do |f| %>
  <%= hidden_field_tag "create_anyway", if has_similar then "true" else "false" end %>
  
  <%= render :partial => 'fields', :locals => { :f => f }  %>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', books_path %>
