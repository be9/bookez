<% form_for :rate, :url => book_rating_url(book) do |f| %>
  <%= f.label :value, "Рейтинг: #{book.rating}" %>
  <%= f.select :value, 1..10 %>
  <%= f.submit book.rated_by_user?(current_user) ? "Оценить заново!" : "Оценить!" %>
<% end %>
